apiVersion: v1
kind: Service
metadata:
  name: teste-cross
  labels:
    app: teste-cross
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: teste-cross